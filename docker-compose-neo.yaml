version: "3.8"

services:
  neo:
    image: neo4j:latest
    ports:
      - 7474:7474
      - ${NEO4J_PORT}:7687
    environment:
      - NEO4J_AUTH=${NEO4J_USER}/${NEO4J_PASS}
    volumes:
      - ./.neo:/data/
